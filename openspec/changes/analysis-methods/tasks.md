## 1. Dependencies

- [x] 1.1 Add `coverage>=7.0,<8` to `[project.dependencies]` in `pyproject.toml` (sole new runtime dependency; do not add `radon` or `astroid`)
- [x] 1.2 Refresh `uv.lock` so `uv sync --locked` passes with the new dependency

## 2. Complexity module

- [x] 2.1 Create `src/snake_eyes/analysis/complexity.py` by lifting the gaze-py McCabe `cyclomatic_complexity` logic, adapting it to pass project gates (full type annotations for mypy --strict, ruff format, import sorting), retaining the gaze-py / Matt Peter / Apache 2.0 provenance header AND adding an Apache-2.0 §4(b) change notice (e.g. `# Modified 2026 by zero-dot-force: extended with Python-specific detection; Go-only pattern lists removed.`)
- [x] 2.2 Implement per-function complexity over `FunctionDef`/`AsyncFunctionDef`, including nested functions and methods of nested classes; exclude lambdas
- [x] 2.3 Emit complexity entries with `name` (unqualified), `package` (dotted module path via shared helper, dropping trailing `.__init__`), `file` (root-relative), `line`, and integer `complexity`; duplicate nested names produce separate entries by `line`
- [x] 2.5 Implement DETERMINISM ordering for complexity: `functions[]` ordered by `(file, line, name)`; the same input tree MUST produce byte-identical JSON-RPC output (mirrors detector's 3.17)
- [x] 2.4 Complexity module skips files that raise `SyntaxError`, `ValueError`, `RecursionError`, `OSError`, or `MemoryError` on parse (BROADENED_EXCEPTIONS tuple: `(SyntaxError, ValueError, RecursionError, OSError, MemoryError)`); stat each candidate before reading — skip non-regular files (`S_ISREG` check; FIFO/device/socket skipped with stderr diagnostic) and enforce the byte-size cap from the stat result; also skip files that exceed the AST depth/recursion budget (RESOURCE_BOUNDS); write a per-file diagnostic to stderr on skip; never abort the whole request

## 3. Detector module

- [x] 3.1 Create `src/snake_eyes/analysis/detector.py` by lifting the gaze-py `detector.py` core logic (pattern constant lists, module-level sentinel-exception collection, per-function AST visitor), adapting it to pass project gates (full type annotations for mypy --strict, ruff format, import sorting), retaining the gaze-py Apache 2.0 provenance header AND adding an Apache-2.0 §4(b) change notice (e.g. `# Modified 2026 by zero-dot-force: extended with Python-specific detection; Go-only pattern lists removed.`)
- [x] 3.2 Delete Go-only pattern lists that no Python rule consumes and do not emit gaze-py internal effect IDs (`se-XXXXXXXX`) in output (zero-waste; keeps coverage achievable)
- [x] 3.3 Implement `analyze_path(root_path, patterns) -> list[FunctionRecord]` that runs `discovery.discover()` and analyzes the ordered concatenation of sorted `source_files` then `test_files`, de-duplicated preserving first occurrence
- [x] 3.4 Implement `analyze_source(source, filename, package) -> list[FunctionRecord]` for single-source unit testing without filesystem access
- [x] 3.5 Emit one `FunctionRecord` per `def`/`async def` (nested included, unqualified names, duplicate names distinguished by `line`); lambdas excluded; effect `location` formatted `"<file>.py:<line>:<col>"`; never set a `classification` field; use `detail` only for Python-specific metadata; `target` and `detail` are OPTIONAL and omitted when not applicable
- [x] 3.6 Implement P0 detection: `ReturnValue` (incl. bare `return`/`return None`), `ErrorReturn` (raise of instance/call/name), `SentinelError` (module-level Exception/BaseException subclass), `ReceiverMutation` (self.x assign/augment/mutating call), `PointerArgMutation` (mutating call or item/attr assign on non-self/cls param)
- [x] 3.7 Implement RAISE_RULE: every `raise` statement emits `ErrorSignal`; a `raise` of an instance/call/name also emits `ErrorReturn`; a bare `raise` (re-raise) dual-emits BOTH `ErrorReturn` and `ErrorSignal`; the SystemExit family (`raise SystemExit`, `raise SystemExit(...)`, `sys.exit`, `os._exit`) emits `ProcessExit` + `ErrorSignal` and MUST NOT emit `ErrorReturn` (`ProcessExit` provides exit semantics; `ErrorReturn` is explicitly suppressed for this family)
- [x] 3.8 Implement `assert` as NOT an effect in v1 (no effect emitted for `assert` statements)
- [x] 3.9 Implement mutation family: `ContainerMutation` (mutating methods on locals/unknown, with `self.*`→`ReceiverMutation` and param→`PointerArgMutation` precedence), `SliceMutation`, `MapMutation`, `GlobalMutation`, `EnvVarMutation`, `ReflectionMutation`, `ClosureCaptureMutation`
- [x] 3.10 Implement output family: `StdoutWrite` (print, sys.stdout.write), `StderrWrite` (sys.stderr.write, print(file=sys.stderr)), `WriterOutput` (write/writelines/flush on non-stdio object), `StreamOutput` (write on file-like opened/passed in, not print; a concretely opened file `f = open(path, "w"); f.write(...)` yields `StreamOutput` co-emitted with `FileSystemWrite`), `HTTPResponseWrite` (framework response name-match)
- [x] 3.11 Implement filesystem family: `FileSystemWrite`, `FileSystemDelete`, `FileSystemMeta` per the mapped call/name rules
- [x] 3.12 Implement control-flow/process family: `ProcessExit` (sys.exit, raise SystemExit, os._exit), `RecoverBehavior` (bare/`except Exception` swallow without re-raise); skip files raising `SyntaxError`, `ValueError`, `RecursionError`, `OSError`, or `MemoryError` (BROADENED_EXCEPTIONS tuple: `(SyntaxError, ValueError, RecursionError, OSError, MemoryError)`); stat each candidate file before reading — skip non-regular files (FIFO, device, socket; `S_ISREG` check) and enforce the byte-size cap from the stat result; write per-file diagnostic to stderr on skip
- [x] 3.13 Implement concurrency family: `GoroutineSpawn`, `ChannelSend`, `MutexOp`, `WaitGroupOp` (Barrier/`asyncio.gather`), `ContextCancellation`, `SyncPoolOp` (`multiprocessing.Pool`)
- [x] 3.14 Implement 10 Python-specific new types: `ErrorSignal`, `GeneratorYield` (yield in non-async), `ContainerMutation`, `StreamOutput`, `AsyncGeneratorYield` (yield in async def), `MetaprogrammingMutation`, `DescriptorEffect`, `ResourceManagement` (`__enter__`/`__exit__`/`__aenter__`/`__aexit__` + `@contextmanager`/`@asynccontextmanager`), `ImportSideEffect` (function-level import only; skip module-level), `MonkeyPatch` (setattr on module or import-alias attr assignment)
- [x] 3.15 Implement remaining families: `DatabaseWrite`, `DatabaseTransaction`, `LogWrite`, `TimeDependency`, `FinalizerRegistration`
- [x] 3.16 Implement NEVER_DROP ambiguity handling: explicitly enumerate the pure-builtin allowlist (or reference the gaze-py constant); `eval`/`exec`/computed `getattr(...)()` emit best-effort effect when a pattern matches, else `CallbackInvocation` with `detail={"confidence": "ambiguous"}`; a call to a name not on the allowlist and not matching a known effect pattern falls back to `CallbackInvocation` with `detail={"confidence": "ambiguous"}`; a statically-resolvable pure local call is NOT an effect; never silently drop any non-pure-builtin call
- [x] 3.17 Implement DETERMINISM ordering: `functions[]` ordered by `(file, line, name)`; `side_effects[]` ordered by `(line, col, type)`; sentinel-exception collection is a deterministic ordered list, never a set; the same input tree MUST produce byte-identical JSON-RPC output
- [x] 3.18 Enforce RESOURCE_BOUNDS in detector: stat each candidate before reading — skip non-regular files (`S_ISREG` check; FIFO/device/socket silently skipped with stderr diagnostic) and enforce the byte-size cap from the stat result; enforce AST depth/recursion budget; a file exceeding a bound is skipped-and-continued (write per-file diagnostic to stderr); also catch `OSError` and `MemoryError` on stat/read alongside `SyntaxError`, `ValueError`, `RecursionError` (BROADENED_EXCEPTIONS tuple: `(SyntaxError, ValueError, RecursionError, OSError, MemoryError)`); never abort the whole request; delegate stat/S_ISREG/byte-cap/AST-depth logic to the centralized safe-file reader helper (5.1) — no per-module duplication
- [x] 3.19 Ensure no fabricated effects: never emit `ChannelClose`, `DeferredReturnMutation`, `AtomicOp`, `CgoCall`, `Panic`, or `UnsafeMutation` without a genuine Python rule match (these are retained as enum vocabulary only)

## 4. Coverage parser module

- [x] 4.1 Create `src/snake_eyes/coverage.py` with `parse_coverage(root_path, patterns) -> list[dict]` (rely on Python 3 absolute imports so `import coverage` resolves to the third-party package, not this module)
- [x] 4.2 Implement lookup order: `root_path/coverage.json` first (stdlib `json`, using `files.<path>.executed_lines`/`missing_lines`), then `root_path/.coverage` via `coverage.Coverage(data_file=...).load()` then `Coverage.analysis2(filename)` (returns executable statements + missing lines so `total_stmts` is computable; NOT `CoverageData.lines()` which returns only executed lines); neither present → return empty list
- [x] 4.3 Implement COVERAGE_CONFINEMENT: run `discover(root_path, patterns)` FIRST; map coverage data ONLY for files in the discovered set; `resolve()` and confine any path from `coverage.json` within `root_path` (never opened verbatim); normalize `coverage.json` keys to root-relative POSIX paths; nonexistent `root_path` → `FileNotFoundError` → `-32602` before any file lookup; malformed/incompatible data (`json.JSONDecodeError`, coverage `CoverageException`, `sqlite3.DatabaseError`) → `parse_coverage` returns bare `[]` (NOT `{"functions": [...]}`; only the handler in `server.py` wraps to `{"functions": []}`); add `(KeyError, TypeError)` to the malformed-data catch so a JSON-valid but wrong-shape `coverage.json` (e.g. missing `files` key, non-dict `files` value, non-list `executed_lines`) also returns `[]`; use `dict.get` with shape-validation (check that each entry is a dict with expected keys before accessing) so no unguarded key access occurs; traversal/out-of-root paths in `coverage.json` keys (e.g. `"../../etc/passwd"` or absolute paths outside `root_path`) are silently ignored and excluded from results
- [x] 4.4 Implement line-to-function mapping via AST spans `[start_line, end_line]`; emit `file`, `function`, `start_line`, `end_line`, `covered_stmts`, `total_stmts`, `percentage` (`round(covered/total*100, 1)` when `total>0` else `0.0`); never run pytest or `coverage run`
- [x] 4.5 Coverage parser skips files that raise `SyntaxError`, `ValueError`, `RecursionError`, `OSError`, or `MemoryError` when parsing AST spans (BROADENED_EXCEPTIONS tuple: `(SyntaxError, ValueError, RecursionError, OSError, MemoryError)`); stat each candidate before reading — skip non-regular files (`S_ISREG` check; FIFO/device/socket skipped with stderr diagnostic) and enforce the byte-size cap from the stat result; write per-file diagnostic to stderr on skip; never abort the whole request
- [x] 4.6 Enforce RESOURCE_BOUNDS in coverage parser: stat each candidate before reading — skip non-regular files (`S_ISREG` check; FIFO/device/socket skipped with stderr diagnostic) and enforce the byte-size cap from the stat result; enforce AST depth/recursion budget; also catch `OSError` and `MemoryError` on stat/read (BROADENED_EXCEPTIONS tuple: `(SyntaxError, ValueError, RecursionError, OSError, MemoryError)`); a file exceeding a bound is skipped-and-continued (write per-file diagnostic to stderr); delegate stat/S_ISREG/byte-cap/AST-depth logic to the centralized safe-file reader helper (5.1) — no per-module duplication; for the `.coverage` branch, run S_ISREG + byte-cap BEFORE calling `Coverage.analysis2()` and wrap `analysis2()` in BROADENED_EXCEPTIONS + `CoverageException(NoSource/NotPython)` for per-file skip
- [x] 4.7 Implement DETERMINISM ordering for coverage: `functions[]` ordered by `(file, start_line, function)`; the same input tree MUST produce byte-identical JSON-RPC output (mirrors detector's 3.17 and complexity's 2.5)

## 5. Shared helpers

- [x] 5.1 Factor a single shared PACKAGE_DERIVATION + function-enumeration helper: strip `.py` suffix, replace `/` with `.`, drop trailing `.__init__`; used by `detector.analyze_path`, `complexity`, and `coverage` — no duplication across modules; ALSO centralize the safe-file reader (stat / `S_ISREG` / `MAX_FILE_BYTES` byte-cap / AST-depth budget) as a single shared utility invoked by 2.4, 3.18, and 4.6 — resolves the "restated per-module vs no-duplication" inconsistency; the safe-file reader raises a typed skip-sentinel (or returns `None`) so callers never re-implement the guard logic; for the `.coverage` branch the safe-file reader must run S_ISREG + byte-cap BEFORE `Coverage.analysis2()` and wrap `analysis2()` in BROADENED_EXCEPTIONS + `CoverageException(NoSource/NotPython)` for per-file skip
- [x] 5.2 Add an `analyze` handler to `server.py` `DEFAULT_DISPATCH` that validates `{root_path, patterns}` (missing/non-string `root_path` → `-32602`, non-array `patterns` → `-32602`), calls `detector.analyze_path`, serializes via `function_record_to_dict`, and maps `FileNotFoundError` → `RpcError(INVALID_PARAMS)`
- [x] 5.3 Add a `complexity` handler to `server.py` `DEFAULT_DISPATCH` with the same param validation, returning `{"functions": [...]}` with `complexity` entries
- [x] 5.4 Add a `coverage` handler to `server.py` `DEFAULT_DISPATCH` with the same param validation, returning `{"functions": [...]}` (empty when no data)
- [x] 5.5 Leave `initialize` capability flags unchanged (`discover: true`; `test_mapping`/`classify_signals`/`streaming: false`); update `src/snake_eyes/analysis/__init__.py` re-exports if needed

## 6. Fixtures

- [x] 6.1 Create `tests/fixtures/effects/p0.py` (return, raise, `self.x=`, `param.append`) with a module-level `class MyError(Exception): pass` (for `SentinelError`, a P0 type); and `tests/fixtures/effects/pure.py` (`def add(a,b): return a+b`; `def noop(): pass`)
- [x] 6.2 Create `tests/fixtures/effects/p1.py` (plain yield, `async def gen(): yield x` for `AsyncGeneratorYield`, `xs.append`, file-object `.write` as a concretely opened file `f = open(path, "w"); f.write(...)` yielding `StreamOutput` co-emitted with `FileSystemWrite` — NOT `WriterOutput`, `global`) and `tests/fixtures/effects/p3.py` (print, `os.environ['X']=`, `sys.exit`)
- [x] 6.3 Create `tests/fixtures/effects/p2.py` (`open(...,'w')`, `setattr`, class with `__enter__`/`__exit__`, class with `__get__`, function-level `importlib.import_module`, `other_module.foo=1`, `type("T", (), {})` for `MetaprogrammingMutation`) and `tests/fixtures/effects/syntax_error.py` (deliberately invalid)
- [x] 6.6 Add ambiguous construct fixture: either a new `tests/fixtures/effects/ambiguous.py` OR extend `p2.py` with (a) `getattr(obj, name)()` [computed attribute call], (b) a call to an unknown external name not on the pure-builtin allowlist, and (c) a statically-resolvable pure local call (e.g. calls another function defined in the same file); used by ambiguity tests in 7.11
- [x] 6.7 Add `tests/fixtures/effects/container_local.py` with a `ContainerMutation` fixture where `xs` is bound LOCALLY (`xs = []; xs.append(...)`) — ensures `xs` is not `self.*` and not a parameter, so the local-container rule applies cleanly
- [x] 6.8 Add `tests/fixtures/coverage/traversal.json`: a `coverage.json` with at least one traversal key (e.g. `"../../etc/passwd"`) and at least one absolute-path key outside `root_path`; used by coverage confinement tests in 7.12
- [x] 6.4 Create a complexity fixture with an `if`/`elif`/`else` + `and` structure; document the exact expected McCabe integer in an arithmetic comment (e.g. `# base 1 + if + elif + and = 4`) — value MUST be hand-derived, not inferred
- [x] 6.5 Create a `.coverage`-only fixture authored via the coverage.py `CoverageData` WRITE API (NOT by running any test suite) under `tests/fixtures/coverage/`; pin `coverage.json` `files.<path>` keys to the module's root-relative discovered path; also create the canned `coverage.json` plus the tiny module it maps to; additionally create a deliberately invalid Python file (`syntax_error.py`) placed beside the covered module under `tests/fixtures/coverage/` so coverage parse-error skip behaviour can be tested (mirrors the detector fixture in 6.3)

## 7. Tests

- [x] 7.1 Create `tests/test_complexity.py` asserting the fixture's exact McCabe integer, nested-function entries, lambda exclusion, `package` derivation (incl. `__init__` → package), and that a `syntax_error.py` in the tree is skipped without aborting the run (BROADENED_EXCEPTIONS)
- [x] 7.2 Create `tests/test_detector.py` with one positive test per new type, cross-referencing each to a specific fixture file and line number: `ErrorSignal`, `GeneratorYield`, `ContainerMutation`, `StreamOutput`, `AsyncGeneratorYield`, `MetaprogrammingMutation`, `DescriptorEffect`, `ResourceManagement`, `ImportSideEffect`, `MonkeyPatch` — assert type string, that `location` contains the fixture filename and line number; consider `pytest.mark.parametrize` for the 10 new-type tests (TC-006)
- [x] 7.3 Add P0 detector tests (`ReturnValue`, `ErrorReturn`, `SentinelError`, `ReceiverMutation`, `PointerArgMutation`, `ErrorSignal`); assert `raise` dual-emits `ErrorReturn` + `ErrorSignal`; assert `noop` yields zero effects; assert `location` (filename+line) on at least one P0 effect; add ≥1 assertion of the full serialized effect shape (target present where applicable, description non-empty); assert `add()` yields exactly ONE `ReturnValue` and NO `PointerArgMutation` on its params (P0 false negatives are release-blocking); assert that ALL THREE SystemExit forms — `raise SystemExit` (no-arg), `sys.exit(0)`, and `os._exit(1)` — each emit `ProcessExit` + `ErrorSignal` and that `ErrorReturn` is ABSENT for all three (test via `analyze_source` inline strings so no fixture file required); assert that a `ReturnValue` effect dict does NOT contain a `target` key (omit-when-None invariant); assert that NONE of the six no-analogue type strings (`ChannelClose`, `DeferredReturnMutation`, `AtomicOp`, `CgoCall`, `Panic`, `UnsafeMutation`) appear in any effect emitted over the full fixture tree
- [x] 7.4 Add a detector test proving a `syntax_error.py` file and a valid file in the same `analyze_path` run yields the valid file's functions with no crash (BROADENED_EXCEPTIONS)
- [x] 7.5 Create `tests/test_coverage.py` asserting: expected `covered_stmts`/`total_stmts`/`percentage` for one function from the canned JSON; missing coverage files return `{"functions": []}`; `.coverage`-only project maps functions at non-100%; both `coverage.json` and `.coverage` present → `coverage.json` wins; malformed coverage data → `parse_coverage` returns bare `[]` (not `{"functions": []}`); a module-shadow guard asserting `import coverage` resolves to the third-party package (e.g. `assert hasattr(coverage, "Coverage")`); assert that when `syntax_error.py` is present alongside valid covered modules (fixture 6.5), it is skipped with a stderr diagnostic while valid coverage entries still return (BROADENED_EXCEPTIONS — coverage was the gap; detector/complexity parity established in 7.1/7.4); assert that the returned `functions` array is ordered by `(file, start_line, function)` — note: the byte-identical test 7.7 CANNOT catch a wrong-but-stable sort key, so this explicit ordering assertion is required; assert that `total_stmts == 0` and `percentage == 0.0` when a function has zero executable statements (zero-total span case)
- [x] 7.6 Create `tests/test_analysis_methods.py` with JSON-RPC end-to-end tests for `analyze`, `complexity`, and `coverage` against `tmp_path` copies of the fixtures (protocol-shaped results; missing `root_path` → `-32602`; effects carry no `classification`; coverage uses `function` field); consider a shared `conftest.py` `tmp_path` fixture (TC-011)
- [x] 7.7 Add a byte-identical determinism test for `analyze`, `complexity`, and `coverage` over a fixture tree: run each method twice on the same input and assert the JSON output is byte-identical
- [x] 7.8 Add schema-conformance canned request/response pair tests for `analyze`, `complexity`, and `coverage` (canned pairs stored as fixtures); canned request/response pairs MUST be HAND-AUTHORED (not generated by running the implementation); each test MUST name the protocol schema version being validated against (e.g. `# validates: gaze analyzer protocol v1.1.0`) and assert full structural equality including array order (not just subset membership); assert that the JSON-RPC `id` in the response matches the request
- [x] 7.9 Add a RESOURCE_BOUNDS test: a pathological file (oversized or deeply nested AST) is skipped and remaining files in the tree still return valid results; also assert that an UNREADABLE file (`PermissionError` / `OSError` on read, simulated via `chmod 000` or `unittest.mock.patch`) is skipped-and-continued while valid files still return; and assert that a NON-REGULAR file (a named FIFO created via `os.mkfifo` with a `.py` extension, e.g. `x.py`) is also skipped-and-continued while valid files still return; parametrize this test across `analyze`, `complexity`, and `coverage` (or assert that all three invoke the centralized safe-file reader helper from 5.1) so bound-enforcement is verified per module
- [x] 7.10 Add a detector-vs-complexity function-set consistency test: assert that the set of `(file, name, line)` tuples from `analyze_path` matches those from `complexity` over the same fixture tree; additionally assert that the coverage `parse_coverage` result function-set is a SUBSET of the detector function-set (coverage can only map functions the detector found; a coverage key outside the detector set is a confinement bug)
- [x] 7.11 Add ambiguity tests (using fixture 6.6 or inline `analyze_source` strings): (a) `getattr(obj, name)()` [computed attr call] => `CallbackInvocation` with `detail['confidence'] == 'ambiguous'` + assert `location` contains filename and line number; (b) unknown external call (name not on pure-builtin allowlist, no known effect pattern) => `CallbackInvocation` with `detail['confidence'] == 'ambiguous'`; (c) statically-resolvable pure local call => zero effects emitted (no `CallbackInvocation`)
- [x] 7.12 Add coverage confinement test (using fixture 6.8): assert that a `coverage.json` containing traversal keys (e.g. `"../../etc/passwd"`) and/or absolute out-of-root-path keys are silently ignored and do NOT appear in the `parse_coverage` result
- [x] 7.13 Add ordering-VALUE assertions for `analyze` and `complexity` (mirroring 7.5's ordering assertion for coverage): (a) `analyze` — assert `functions[]` is ordered by `(file, line, name)` and `side_effects[]` within each function is ordered by `(line, col, type)`; use a fixture or inline source that produces at least two effects at the SAME `(line, col)` to exercise the `type` tiebreak; (b) `complexity` — assert `functions[]` is ordered by `(file, line, name)`; note: the byte-identical test 7.7 CANNOT catch a wrong-but-stable sort key, so these explicit ordering assertions are required
- [x] 7.14 Add double-emit NEGATIVE assertions (assert the WRONG type is absent, not just the right type present): (a) `f.write(...)` on a concretely opened file => `StreamOutput` (+ `FileSystemWrite`) and NOT `WriterOutput`; (b) `self.x.append(...)` => `ReceiverMutation` and NOT `ContainerMutation`; (c) `param.append(...)` (where `param` is a function parameter) => `PointerArgMutation` and NOT `ContainerMutation`; (d) `print(...)` => `StdoutWrite` and NOT `StreamOutput`

## 8. Documentation

- [x] 8.1 Sync `README.md`: update the status table to show `analyze`/`complexity`/`coverage` as now implemented; note `coverage.py` is now a runtime dependency; update the 'planned later' note (complexity.py and coverage.py delivered here, not planned); correct README.md lines ~19–20 so that side-effect detection is described as IMPLEMENTED (not a "later issue"), radon is described as REJECTED/not used (not "planned later"), and astroid remains described as planned
- [x] 8.2 Sync `AGENTS.md`: update Technology Stack to reflect radon is NOT a dependency (lifted gaze-py McCabe used instead); update Project Structure to move `complexity.py`/`coverage.py` from 'Planned later' to delivered, and add `detector.py`; correct the Architecture "Snake Eyes owns" list (~L65) so that cyclomatic complexity no longer attributes radon as the implementation mechanism; drop the "(planned)" qualifier from the Technology Stack `coverage.py` line (~L79) since `coverage>=7.0,<8` is now a shipped, pinned runtime dependency

## 9. Verification

- [x] 9.1 Run `uv sync --locked`
- [x] 9.2 Run `uv run ruff check src/ tests/` and `uv run ruff format --check src/ tests/`
- [x] 9.3 Run `uv run mypy src/`
- [x] 9.4 Run `uv run pytest --cov=snake_eyes --cov-report=term-missing --cov-fail-under=85` (target: `complexity.py` 100%, `detector.py` ≥90%, `coverage.py` ≥90% — confirm via `term-missing` output; project gate remains ≥85% and MUST NOT be lowered)
- [x] 9.5 Manually verify `uv run snake-eyes --stdio` answers `analyze`, `complexity`, and `coverage` with protocol-shaped JSON and still reports unchanged `initialize` capabilities

<!-- spec-review: passed -->
<!-- code-review: passed -->
