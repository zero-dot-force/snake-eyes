[project]
name = "snake-eyes"
dynamic = ["version"]
description = "Python side-effect analyzer backend for Gaze (JSON-RPC 2.0 external analyzer protocol)"
readme = "README.md"
requires-python = ">=3.11"
license = "Apache-2.0"
dependencies = ["astroid>=3.0,<4", "coverage>=7.0,<8"]

[project.scripts]
snake-eyes = "snake_eyes.__main__:main"

[dependency-groups]
dev = [
    "pytest",
    "pytest-cov",
    "mypy",
    "ruff",
]

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "src/snake_eyes/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/snake_eyes"]

[tool.ruff]
line-length = 88
target-version = "py311"
exclude = [
    "tests/fixtures/effects/syntax_error.py",
    "tests/fixtures/coverage/syntax_error.py",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP"]

[tool.ruff.lint.per-file-ignores]
"tests/fixtures/**" = ["E501", "F401", "I001", "UP", "E999"]

[tool.mypy]
strict = true

[tool.coverage.run]
branch = true

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "--cov=snake_eyes --cov-report=term-missing --cov-fail-under=85"
markers = ["slow: marks tests that spawn subprocesses (deselect with -m 'not slow')"]
